extends layout.pug

include utils.pug

block content
  +validationErrorSummary(errors)
  form(action=`/users/edit/${user.id}` method='post')
    include user-form.pug
    div(class='py-4')
      button(type='submit' class='btn btn-primary') Update User
      a(href=`/users/${user.id}` class='btn btn-warning ml-2') Cancel
